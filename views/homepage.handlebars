{{!-- This page extends the main layout --}}
<div class="container mx-auto p-4">
    <h1 class="text-5xl font-bold mb-8 text-left italic" style="font-family: 'Roboto', sans-serif;">
        Step into a world of sophistication and drive your dream car with <span style="
            background: linear-gradient(90deg, #FFD700, #FFA500, #FFD700);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-weight: bold;
            text-shadow: 0 1px 0 #b38600, 1px 2px 1px #999999, 1px 3px 1px #666666, 2px 4px 1px #333333, 3px 5px 3px #111111;">
            Lux
        </span>.
    </h1>
    <div class="carousel">
        {{#each cars as |car|}}
        <div class="bg-white shadow-lg hover:shadow-2xl rounded-lg p-6 w-64 h-80 transition-shadow duration-300">
          {{!-- TO DO: find more images for the remainder of the car models and colors in the database;
            there won't be time to do this but I thought I'd leave a note acknowledging that it should be done if I were to keep iterating on the project past turn-in --}}
            <img src="{{car.image}}" alt="{{car.model}}" class="w-full h-48 object-cover rounded-t-lg">
            <h2 class="text-xl font-bold mb-2 italic">{{car.model}}</h2>
            <p class="text-gray-700"> Color: {{car.color}}, Availability: {{car.availability}}, Manufacturer: {{car.manufacturer}}</p>
        </div>
        {{/each}}
    </div>
</div>
